export const defaultPages = {
    page1: {
        title: "PEARL NUM",
        content: `<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8" /> <meta name="viewport" content="width=device-width, initial-scale=1.0"/> <title>Wingo Oracle [Precision AI]</title> <style> body { font-family: 'Segoe UI', Roboto, sans-serif; background-color: #050505; color: #fff; margin: 0; padding: 20px; display: flex; flex-direction: column; align-items: center; } .container { background: #111; padding: 25px; border-radius: 20px; width: 95%; max-width: 500px; border: 2px solid #222; text-align: center; box-shadow: 0 0 60px rgba(255, 215, 0, 0.15); } .display-box { background: linear-gradient(180deg, #1a1a1a 0%, #000 100%); border: 2px solid #333; padding: 30px; border-radius: 15px; margin-bottom: 20px; position: relative; } .pred-type { font-size: 3.5em; font-weight: 900; margin: 10px 0; line-height: 1; text-transform: uppercase; } .pred-sub { font-size: 1.1em; color: #aaa; letter-spacing: 1px; } .highlight-num { color: #fff; font-weight: bold; border-bottom: 2px solid #ffd700; } .analysis-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-bottom: 20px; } .a-box { background: #1a1a1a; padding: 10px; border-radius: 8px; border: 1px solid #333; } .a-lbl { font-size: 0.7em; color: #666; display: block; text-transform: uppercase; } .a-val { font-weight: bold; font-size: 1.1em; } .input-group { display: flex; gap: 10px; margin-bottom: 15px; } input { width: 100%; padding: 12px; background: #222; border: 1px solid #444; color: white; text-align: center; border-radius: 8px; font-size: 1.2em; } button { width: 100%; padding: 15px; background: linear-gradient(90deg, #ffd700, #e1b12c); color: #000; border: none; border-radius: 8px; font-weight: bold; font-size: 1.1em; cursor: pointer; margin-bottom: 15px; } .btn-auto { background: #333; color: #fff; border: 1px solid #555; font-size: 0.9em; padding: 10px; width: auto; margin: 0; } .logs { height: 180px; overflow-y: auto; background: #000; border: 1px solid #222; border-radius: 8px; padding: 10px; text-align: left; font-family: monospace; font-size: 0.9em; } .row { display: flex; justify-content: space-between; padding: 6px 0; border-bottom: 1px solid #111; } .c-Big { color: #ff7b72; } .c-Small { color: #3fb950; } .c-Wait { color: #444; } .win { color: #00e676; } .loss { color: #ff4757; } .safe-zone { color: #ffd700; font-weight: bold; } </style> </head> <body> <div class="container"> <h2 style="margin:0 0 10px 0; color:#ffd700;">👁️ PRECISION ORACLE</h2> <div class="input-group"> <input type="number" id="periodInput" placeholder="Next 3-digit period"> <input type="number" id="resultInput" placeholder="Current number"> </div> <button onclick="manualPredict()">ANALYZE & PREDICT</button> <div class="display-box" id="mainBox"> <div style="font-size:0.8em; color:#666;">AI CONFIDENCE</div> <div id="predText" class="pred-type c-Wait">---</div> <div id="predNum" class="pred-sub">Best Numbers: <span class="highlight-num">-</span></div> </div> <div class="analysis-grid"> <div class="a-box"> <span class="a-lbl">MATH SIGNAL</span> <span id="mathSig" class="a-val">-</span> </div> <div class="a-box"> <span class="a-lbl">MEMORY PATTERN</span> <span id="memSig" class="a-val">-</span> </div> </div> <div style="display:flex; gap:10px; justify-content:center; margin-bottom:15px; align-items:center;"> <button class="btn-auto" onclick="toggleAuto()">🔄 START AUTO-FETCH</button> </div> <div id="logBox" class="logs"> <div style="text-align:center; padding:20px; color:#333;">History Empty</div> </div> </div> <script> // STATE let historyDB = []; // Stores past results let activePrediction = null; let lastProcessedPeriod = -1; let lastResult = -1; let autoInterval = null; // --- PATTERN MEMORY ENGINE --- function analyzeHistory(currentNum) { if (historyDB.length < 10) return "Neutral"; let bigCount = 0, smallCount = 0, matches = 0; for (let i = 1; i < historyDB.length; i++) { const prevNum = historyDB[i].num; const nextResult = historyDB[i-1].num; if (prevNum === currentNum) { matches++; if (nextResult >= 5) bigCount++; else smallCount++; } } if (matches === 0) return "Neutral"; if (bigCount > smallCount) return "Big"; if (smallCount > bigCount) return "Small"; return "Neutral"; } // --- MATH LOGIC --- function calculateMath(periodStr, lastResult) { const pDigit = parseInt(String(periodStr).slice(-1)) || 0; let raw = (pDigit + lastResult + 1); let final = raw % 10; return final >= 5 ? "Big" : "Small"; } // --- NUMBER PREDICTOR --- function predictNumbers(size, lastResult) { let nums = []; if (size === "Big") { nums.push((lastResult + 2) % 10); nums.push(lastResult === 9 ? 5 : lastResult + 1); if (nums[0] < 5) nums[0] = 9; } else { nums.push(Math.abs(lastResult - 2) % 5); nums.push(lastResult === 0 ? 4 : lastResult - 1); } return nums; } // --- MASTER PREDICTOR --- function runPrediction(period, result) { const mathSig = calculateMath(period, result); const memSig = analyzeHistory(result); let final = "Skip"; if (memSig === "Neutral") final = mathSig; else if (mathSig === memSig) final = mathSig; const nums = predictNumbers(final === "Skip" ? mathSig : final, result); return { math: mathSig, mem: memSig, final: final, nums: nums }; } // --- UI UPDATE --- function updateUI(targetPeriod, data) { const mathEl = document.getElementById('mathSig'); const memEl = document.getElementById('memSig'); mathEl.innerText = data.math; mathEl.className = "a-val c-" + data.math; memEl.innerText = data.mem; memEl.className = "a-val c-" + data.mem; const mainEl = document.getElementById('predText'); const box = document.getElementById('mainBox'); const numEl = document.getElementById('predNum'); if (data.final === "Skip") { mainEl.innerText = "WAIT"; mainEl.className = "pred-type c-Wait"; box.style.borderColor = "#333"; numEl.innerHTML = "Conflict detected. Skipping."; } else { mainEl.innerText = data.final; mainEl.className = "pred-type c-" + data.final; box.style.borderColor = data.final === "Big" ? "#ff7b72" : "#3fb950"; numEl.innerHTML = \`Target ...\${targetPeriod.toString().slice(-3)}: <span class="highlight-num">\${data.nums.join(", ")}</span>\`; } activePrediction = { period: targetPeriod, pick: data.final, nums: data.nums }; } // --- MANUAL PREDICT (keeps previous behaviour) --- function manualPredict() { const pVal = document.getElementById('periodInput').value; const rVal = parseInt(document.getElementById('resultInput').value); // User-provided pVal might be 3-digit short; store as-is if (!pVal || isNaN(rVal)) return alert("Enter period and result to predict."); historyDB.unshift({ period: parseInt(pVal), num: rVal }); if (historyDB.length > 50) historyDB.pop(); const pred = runPrediction(pVal, rVal); updateUI(parseInt(pVal) + 1, pred); } // --- AUTO FETCH (no proxy) --- async function fetchData() { const ts = Date.now(); const url = \`https://draw.ar-lottery01.com/WinGo/WinGo_1M/GetHistoryIssuePage.json?ts=\${ts}\`; try { const res = await fetch(url, { cache: "no-store" }); const data = await res.json(); if (!data?.data?.list?.length) return; const latest = data.data.list[0]; const serverPeriod = parseInt(latest.issueNumber); const resultNum = parseInt(latest.number); if (isNaN(serverPeriod) || isNaN(resultNum)) return; const outcome = resultNum >= 5 ? "Big" : "Small"; // Consider update when period OR result changed const isNewUpdate = (serverPeriod !== lastProcessedPeriod) || (resultNum !== lastResult); if (!isNewUpdate) return; // update trackers immediately so repeated quick fetches won't re-process same values lastProcessedPeriod = serverPeriod; lastResult = resultNum; // Update UI - show next period's last 3 digits const nextPeriod = serverPeriod + 1; document.getElementById('periodInput').value = nextPeriod.toString().slice(-3); document.getElementById('resultInput').value = resultNum; // Add to memory (keep recent 100) historyDB.unshift({ period: serverPeriod, num: resultNum }); if (historyDB.length > 100) historyDB.pop(); // Check WIN for previous activePrediction if (activePrediction && activePrediction.period === serverPeriod && activePrediction.pick !== "Skip") { const won = (activePrediction.pick === outcome); const exact = activePrediction.nums.includes(resultNum); addLog(serverPeriod, activePrediction.pick, outcome, won, exact); } // Generate prediction for next const pred = runPrediction(serverPeriod, resultNum); updateUI(nextPeriod, pred); } catch (err) { console.log("Fetch error:", err); } } // --- LOGS --- function addLog(period, pick, res, won, exact) { const box = document.getElementById('logBox'); if (box.innerText.includes("History Empty") || box.innerText.includes("Empty")) box.innerHTML = ""; const div = document.createElement('div'); div.className = "row"; let color = won ? "win" : "loss"; let status = won ? "WIN" : "LOSS"; if (exact) { status = "JACKPOT"; color = "safe-zone"; } div.innerHTML = \`<span>...\${period.toString().slice(-4)}</span><span>\${pick} vs \${res}</span><span class="\${color}">\${status}</span>\`; box.prepend(div); } // --- AUTO TOGGLE --- function toggleAuto() { const btn = document.querySelector('.btn-auto'); if (autoInterval) { clearInterval(autoInterval); autoInterval = null; btn.innerText = "🔄 START AUTO-FETCH"; } else { // immediate fetch + repetitive fetchData(); autoInterval = setInterval(fetchData, 3000); btn.innerText = "⛔ STOP AUTO-FETCH"; } } // Optionally: run a single fetch once page loads // fetchData(); </script> </body> </html>`,
    },
    page2: {
        title: "S/M GOLD",
        content: `<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>Real-Time Number Prediction</title> <style> /* CSS STYLES */ body { font-family: 'Arial', sans-serif; display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 100vh; margin: 0; background-color: #f4f4f9; } .container { background: white; padding: 30px; border-radius: 12px; box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1); text-align: center; width: 90%; max-width: 400px; } h1 { color: #333; margin-bottom: 10px; font-size: 1.8em; } p { color: #666; margin-bottom: 25px; font-size: 0.9em; } /* Input and Button Styling */ #numberInput { padding: 12px; margin-right: 10px; border: 2px solid #ccc; border-radius: 8px; font-size: 18px; width: 80px; text-align: center; box-sizing: border-box; } #predictButton { padding: 12px 25px; background-color: #ff5722; /* Vibrant Orange */ color: white; border: none; border-radius: 8px; font-size: 18px; cursor: pointer; transition: background-color 0.3s, transform 0.1s; } #predictButton:hover { background-color: #e64a19; } #predictButton:active { transform: scale(0.98); } /* Result and Animation Styling */ #resultContainer { margin-top: 30px; height: 120px; /* Space for the result */ display: flex; flex-direction: column; align-items: center; justify-content: center; overflow: hidden; border-top: 1px solid #eee; padding-top: 20px; } .result { font-size: 3.5em; font-weight: bold; display: flex; align-items: center; opacity: 0; /* Hidden by default */ transform: translateY(20px); transition: opacity 0.5s ease-out, transform 0.5s ease-out; } .result.show { opacity: 1; transform: translateY(0); } .result-digit { padding: 8px 15px; border-radius: 50%; color: white; margin-right: 15px; font-size: 0.8em; } .result-text { font-size: 1em; } /* Color classes for BIG/SMALL */ .color-big { background-color: #dc3545; /* Red */ } .text-big { color: #dc3545; } .color-small { background-color: #28a745; /* Green */ } .text-small { color: #28a745; } /* Animation */ @keyframes loading-spin { 0% { transform: rotate(0deg); opacity: 1; } 50% { opacity: 0.5; } 100% { transform: rotate(360deg); opacity: 1; } } .animation { font-size: 3em; color: #ffc107; animation: loading-spin 1.2s ease-in-out infinite; } .hidden { display: none; } </style> </head> <body> <div class="container"> <h1>⏰ Live Minute Calculation</h1> <p>.</p> <div> <input type="number" id="numberInput" placeholder="0-9" min="0" max="9"> <button id="predictButton">Calculate Outcome</button> </div> <div id="resultContainer"> <div id="animationArea" class="hidden"> <span class="animation">🌀</span> </div> <div id="resultOutput" class="result"> </div> </div> </div> <script> // JAVASCRIPT LOGIC document.getElementById('predictButton').addEventListener('click', function() { const inputElement = document.getElementById('numberInput'); const resultOutput = document.getElementById('resultOutput'); const animationArea = document.getElementById('animationArea'); // 1. Get the user's number and validate let userNumber = parseInt(inputElement.value); if (isNaN(userNumber) || userNumber < 0 || userNumber > 9) { alert("Please enter a single digit number (0-9)."); return; } // Reset previous results resultOutput.classList.remove('show'); resultOutput.innerHTML = ''; // 2. Start Animation animationArea.classList.remove('hidden'); // 3. Perform the calculation logic after a short delay (for the animation) setTimeout(() => { // Get the ACTUAL current minute's last digit const currentMinute = new Date().getMinutes(); const timeMinuteLastDigit = currentMinute % 10; // Calculation: (User Number + Minute Last Digit) -> Final Digit let sum = userNumber + timeMinuteLastDigit; let finalDigit = sum % 10; // Get the last digit // 4. Determine the outcome let outcome = ''; let colorClass = ''; // Rule: 0 to 4 = Big, 5 to 9 = Small if (finalDigit >= 0 && finalDigit <= 4) { outcome = 'BIG'; colorClass = 'big'; } else { // 5, 6, 7, 8, 9 outcome = 'SMALL'; colorClass = 'small'; } // 5. Display simplified results and stop animation animationArea.classList.add('hidden'); // Construct the simplified output HTML resultOutput.innerHTML = \` <span class="result-digit color-\${colorClass}">\${finalDigit}</span> <span class="result-text text-\${colorClass}">\${outcome}</span> \`; resultOutput.classList.add('show'); }, 1500); // 1.5-second animation delay }); </script> </body> </html>`,
    },
    page3: {
        title: "Dual core",
        content: `<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>Dual-Core + Custom Input</title> <style> body { font-family: 'Arial', sans-serif; background-color: #2c3e50; color: white; display: flex; justify-content: center; align-items: center; min-height: 100vh; margin: 0; } .container { background: #34495e; padding: 25px; border-radius: 15px; box-shadow: 0 10px 30px rgba(0,0,0,0.3); text-align: center; width: 90%; max-width: 450px; } h2 { margin-top: 0; color: #ecf0f1; font-size: 1.5em; } /* Custom Input Styling */ .input-group { margin: 20px 0; background: #2c3e50; padding: 15px; border-radius: 10px; border: 1px solid #465c71; } label { display: block; margin-bottom: 8px; font-size: 0.9em; color: #bdc3c7; } input[type="number"] { width: 80%; padding: 12px; font-size: 18px; text-align: center; border-radius: 8px; border: 2px solid #f1c40f; /* Yellow border to stand out */ background-color: #fff; color: #333; outline: none; } /* Result Grid */ .grid-container { display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin-top: 20px; } .card { background: #2c3e50; padding: 15px; border-radius: 10px; border: 1px solid #465c71; } .card-title { font-size: 0.8em; color: #bdc3c7; text-transform: uppercase; letter-spacing: 1px; } .card-value { font-size: 2.5em; font-weight: bold; margin: 10px 0; } /* Colors */ .txt-green { color: #2ecc71; } .txt-red { color: #e74c3c; } #calcBtn { width: 100%; padding: 15px; background: #3498db; color: white; border: none; border-radius: 8px; font-size: 18px; font-weight: bold; cursor: pointer; transition: 0.2s; } #calcBtn:hover { background: #2980b9; } /* Confirmation Box */ .confirmation-box { margin-top: 20px; padding: 15px; border-radius: 8px; display: none; } .high-match { background: rgba(46, 204, 113, 0.2); border: 1px solid #2ecc71; } .low-match { background: rgba(231, 76, 60, 0.2); border: 1px solid #e74c3c; } </style> </head> <body> <div class="container"> <h2>⚔️ Dual-Core + Custom</h2> <div class="input-group"> <label>Add Custom Number (Offset)</label> <input type="number" id="customInput" placeholder="0" value="0"> </div> <button id="calcBtn">ANALYZE NOW</button> <div id="resultsArea" style="display:none;"> <div class="grid-container"> <div class="card"> <div class="card-title">Number Formula</div> <div id="numResult" class="card-value">-</div> <div id="numSize" style="font-size:0.9em;">...</div> </div> <div class="card"> <div class="card-title">Size Formula</div> <div id="sizeResult" class="card-value" style="font-size:1.8em; line-height: 1.4em;">-</div> </div> </div> <div id="signalBox" class="confirmation-box"> <strong id="signalTitle" style="display:block; font-size:1.2em; margin-bottom:5px;"></strong> <span id="signalDesc" style="font-size:0.9em;"></span> </div> </div> </div> <script> document.getElementById('calcBtn').addEventListener('click', function() { const customInput = document.getElementById('customInput'); const resultsArea = document.getElementById('resultsArea'); const numResultDisplay = document.getElementById('numResult'); const numSizeDisplay = document.getElementById('numSize'); const sizeResultDisplay = document.getElementById('sizeResult'); const signalBox = document.getElementById('signalBox'); const signalTitle = document.getElementById('signalTitle'); const signalDesc = document.getElementById('signalDesc'); // 1. Get Time Data const now = new Date(); const minute = now.getMinutes(); const hour = now.getHours(); const M = minute % 10; const H = hour % 10; // 2. Get Custom Input (Default to 0 if empty) let adjustment = parseInt(customInput.value); if (isNaN(adjustment)) adjustment = 0; // --- STRATEGY 1: EXACT NUMBER (The Lag + Adjustment) --- // Formula: (Minute - 1) + Adjustment let rawNum = (M - 1) + adjustment; // Handle negative wrapping (e.g., -1 becomes 9) let predNum = ((rawNum % 10) + 10) % 10; // Determine if that number is Big or Small let predNumIsBig = (predNum >= 5); let predNumSizeText = predNumIsBig ? "BIG" : "SMALL"; let predNumColor = predNumIsBig ? "txt-red" : "txt-green"; // --- STRATEGY 2: SIZE ONLY (Golden Cross + 2 + Adjustment) --- // Formula: (Minute + Hour + 2) + Adjustment let sum = M + H + 2 + adjustment; let finalDigit = ((sum % 10) + 10) % 10; // Secure Modulo let strategyIsBig = (finalDigit >= 5); let strategySizeText = strategyIsBig ? "BIG" : "SMALL"; let strategyColor = strategyIsBig ? "txt-red" : "txt-green"; // 3. Display Results resultsArea.style.display = "block"; // Update Box 1 numResultDisplay.innerText = predNum; numResultDisplay.className = "card-value " + predNumColor; numSizeDisplay.innerText = "(" + predNumSizeText + ")"; // Update Box 2 sizeResultDisplay.innerText = strategySizeText; sizeResultDisplay.className = "card-value " + strategyColor; // 4. Calculate Signal Strength signalBox.style.display = "block"; if (predNumSizeText === strategySizeText) { // MATCH! signalBox.className = "confirmation-box high-match"; signalTitle.innerText = "✅ STRONG SIGNAL"; signalTitle.style.color = "#2ecc71"; signalDesc.innerText = \`Both formulas (with your +\${adjustment} add) agree on \${strategySizeText}.\`; } else { // MISMATCH signalBox.className = "confirmation-box low-match"; signalTitle.innerText = "⚠️ CONFLICT"; signalTitle.style.color = "#e74c3c"; signalDesc.innerText = \`Number suggests \${predNumSizeText}, but Size Formula suggests \${strategySizeText}.\`; } }); </script> </body> </html>`,
    },
    page4: {
        title: "Evening bet",
        content: `<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>Wingo Analyst [Timer-Lock]</title> <style> /* --- DARK THEME UI --- */ body { font-family: 'Segoe UI', sans-serif; background-color: #050505; color: #fff; margin: 0; padding: 10px; display: flex; flex-direction: column; align-items: center; } .container { background: #111; padding: 15px; border-radius: 12px; width: 95%; max-width: 500px; border: 1px solid #333; text-align: center; } /* HEADER & STATUS */ .top-bar { display: flex; justify-content: space-between; font-size: 0.8em; color: #777; margin-bottom: 10px; } /* SPLIT VIEW */ .split-box { display: flex; gap: 10px; margin-bottom: 15px; } .half { flex: 1; background: #1a1a1a; padding: 10px; border-radius: 8px; border: 1px solid #333; } .lbl { font-size: 0.6em; color: #aaa; text-transform: uppercase; display: block; margin-bottom: 5px; } .big-txt { font-weight: 900; font-size: 1.5em; } /* STRATEGY GRID */ .grid { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 5px; margin-bottom: 15px; } .card { background: #222; padding: 8px; border-radius: 6px; } .c-title { font-size: 0.6em; color: #888; display: block; text-transform: uppercase; } .c-val { font-weight: bold; font-size: 1.1em; } .b-gold { border-bottom: 2px solid gold; } .b-silv { border-bottom: 2px solid silver; } .b-bron { border-bottom: 2px solid #cd7f32; } /* LOGS */ .logs { height: 250px; overflow-y: auto; background: #000; border: 1px solid #333; border-radius: 8px; padding: 10px; text-align: left; font-family: monospace; font-size: 0.85em; } .row { border-bottom: 1px solid #222; padding: 5px 0; display: flex; justify-content: space-between; } .win { color: #00e676; } .loss { color: #ff4757; } .skip { color: #555; } /* UTILS */ .c-Big { color: #ff4757; } .c-Small { color: #00e676; } .c-Wait { color: #555; } .controls { margin-bottom: 10px; } button { background: #2980b9; color: white; border: none; padding: 8px 20px; border-radius: 4px; cursor: pointer; font-weight: bold;} button:active { transform: scale(0.95); } .timer-box { font-family: monospace; color: #f1c40f; font-weight: bold; } </style> </head> <body> <div class="container"> <div class="top-bar"> <span>CLOCK: <span id="clock" style="color:white;">--:--:--</span></span> <span class="timer-box" id="nextScan">Next: --s</span> </div> <div class="split-box"> <div class="half" style="border-color: #444;"> <span class="lbl">LAST RESULT</span> <div id="lastResDisplay" style="font-size:1.2em; font-weight:bold;">---</div> <div id="lastPerDisplay" style="font-size:0.8em; color:#666;">...</div> </div> <div class="half" style="border-color: #00e676;"> <span class="lbl">NEXT TARGET</span> <div id="mainPred" class="big-txt c-Wait">---</div> <div id="nextPerDisplay" style="font-size:0.8em; color:#00e676;">...</div> </div> </div> <div class="grid"> <div class="card b-gold"><span class="c-title">🥇 Link</span><span id="gVal" class="c-val">-</span></div> <div class="card b-silv"><span class="c-title">🥈 Neural</span><span id="sVal" class="c-val">-</span></div> <div class="card b-bron"><span class="c-title">🥉 Time</span><span id="bVal" class="c-val">-</span></div> </div> <div class="controls"> <button onclick="fetchData()">Force Manual Fetch</button> </div> <div id="logBox" class="logs"> <div style="text-align:center; padding:20px; color:#444;">Waiting for :02 seconds...</div> </div> </div> <script> // --- STATE --- let lastKnownPeriod = "0"; // String to prevent math error let activePrediction = null; let hasFetchedThisMinute = false; // Flag to prevent double fetch // --- 1. MATH LOGIC --- function calculate(periodStr, resultNum) { const min = new Date().getMinutes() % 10; const curP_LastDigit = parseInt(periodStr.slice(-1)); const nextP_LastDigit = (curP_LastDigit + 1) % 10; const s1 = ((nextP_LastDigit + resultNum) % 10) >= 5 ? "Big" : "Small"; let v2 = (resultNum * 2) + min; if(resultNum === 0 || resultNum === 5) v2 += 1; const s2 = (v2 % 10) >= 5 ? "Big" : "Small"; const s3 = Math.abs(min - resultNum) >= 5 ? "Big" : "Small"; // Consensus const bigs = [s1, s2, s3].filter(x => x === "Big").length; const final = bigs >= 2 ? "Big" : "Small"; return { gold: s1, silver: s2, bronze: s3, final: final }; } // --- 2. FETCH FUNCTION (Direct - No Proxy) --- async function fetchData() { const ts = Date.now(); const url = \`https://draw.ar-lottery01.com/WinGo/WinGo_1M/GetHistoryIssuePage.json?ts=\${ts}\`; try { // Visual Indicator that fetch is happening document.getElementById('nextScan').innerText = "FETCHING..."; document.getElementById('nextScan').style.color = "#00e676"; const res = await fetch(url); const data = await res.json(); if(data?.data?.list?.length) { const latest = data.data.list[0]; const serverPeriodStr = String(latest.issueNumber); const resultNum = parseInt(latest.number); const resultOutcome = resultNum >= 5 ? "Big" : "Small"; // CHECK IF NEW PERIOD if (serverPeriodStr !== lastKnownPeriod) { // 1. Check Win/Loss of Previous if (activePrediction) { if (activePrediction.period === serverPeriodStr) { const isWin = (activePrediction.pick === resultOutcome); addLog(serverPeriodStr, \`\${activePrediction.pick} vs \${resultOutcome}\`, isWin ? "win" : "loss"); } else { // If period ID jumped (Network lag) addLog(activePrediction.period, "Skipped", "skip"); } } // 2. Update State lastKnownPeriod = serverPeriodStr; // 3. Calc Next Target // Use BigInt logic for incrementing string ID safely const nextPeriodBigInt = BigInt(serverPeriodStr) + 1n; const targetPeriodStr = nextPeriodBigInt.toString(); const strats = calculate(serverPeriodStr, resultNum); // 4. Update UI document.getElementById('lastResDisplay').innerText = \`\${resultNum} (\${resultOutcome})\`; document.getElementById('lastResDisplay').className = "c-" + resultOutcome; document.getElementById('lastPerDisplay').innerText = "..." + serverPeriodStr.slice(-4); document.getElementById('mainPred').innerText = strats.final; document.getElementById('mainPred').className = "big-txt c-" + strats.final; document.getElementById('nextPerDisplay').innerText = "..." + targetPeriodStr.slice(-4); updateCard('gVal', strats.gold); updateCard('sVal', strats.silver); updateCard('bVal', strats.bronze); // 5. Store Prediction activePrediction = { period: targetPeriodStr, pick: strats.final }; } } } catch(e) { console.error("Fetch Error:", e); document.getElementById('nextScan').innerText = "ERROR"; document.getElementById('nextScan').style.color = "#ff4757"; } } // --- 3. TIMING ENGINE (The Lock) --- setInterval(() => { const now = new Date(); const s = now.getSeconds(); // Update Clock UI document.getElementById('clock').innerText = now.toLocaleTimeString('en-US',{hour12:false}); // Countdown UI let diff = 62 - s; if(diff > 60) diff -= 60; if(document.getElementById('nextScan').innerText !== "FETCHING...") { document.getElementById('nextScan').innerText = \`Scan in: \${diff}s\`; document.getElementById('nextScan').style.color = "#f1c40f"; } // RESET FLAG at Second 50 if (s > 50) { hasFetchedThisMinute = false; } // TRIGGER AT SECOND 02 if (s === 2 && !hasFetchedThisMinute) { hasFetchedThisMinute = true; fetchData(); } }, 1000); // Check every second // --- UI HELPERS --- function updateCard(id, val) { const el = document.getElementById(id); el.innerText = val; el.className = "c-val c-" + val; } function addLog(periodStr, text, type) { const box = document.getElementById('logBox'); if(box.innerText.includes("Waiting")) box.innerHTML = ""; const div = document.createElement('div'); div.className = "row"; const pStr = String(periodStr).slice(-4); const icon = type === 'win' ? 'WIN 🟢' : (type === 'loss' ? 'LOSS 🔴' : 'SKIP ⚪'); div.innerHTML = \`<span>...\${pStr}</span><span>\${text}</span><span class="\${type}">\${icon}</span>\`; box.prepend(div); } // Initial Fetch on Load (so you don't wait 1 minute to see data) fetchData(); </script> </body> </html>`,
    },
    page5: {
        title: "Page 5",
        content: ""
    }
};
